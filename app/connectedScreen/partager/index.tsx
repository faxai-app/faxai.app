import React from "react";
import { StyleSheet, Text, View } from "react-native";

export default function Partager() {
  return (
    <View style={styles.container}>
      <Text>Salut</Text>
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: "#e2db76ff",
  },
});
